// import React from 'react';
// import { shallow } from 'enzyme';

// import CleanTextArea from '../index';

describe('<CleanTextArea />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
